{
  "scripts": {
    "get:articles": "node -e 'require(\"./blog/getArticles\").getArticles()'",
    "fetch": "git fetch",
    "pull": "git pull",
    "push": "git add -A && git commit -m '.' && git push origin master"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^10.0.0",
    "storyblok-js-client": "^4.1.4"
  }
}

{
  "scripts": {
    "get:blog": "node -e 'require(\"./utils/api\").getBlog()'",
    "fetch": "git fetch",
    "pull": "git pull",
    "push": "git add -A && git commit -m '.' && git push origin master"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^10.0.0",
    "storyblok-js-client": "^4.1.4"
  }
}
